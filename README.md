# Computational-Fluid-Dynamics
An interactive Eulerian CFD-Solver

To compile use:
gcc cfd.c -o cfd -lm `sdl2-config --cflags --libs` && ./cfd 

Special recognition goes to Matthias Müller whose code and Youtube Video were the base of my project
